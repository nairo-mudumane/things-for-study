parte tecnica:
	front end [react]:
	componente funcional
	hooks
	context
	ccs: styled-components;stitches;
	tools:radix-ui;
	data-fetching:
		swr;
		react-query;
		graphQL: uRQL;apollo-client;relay;
	state-management: context;reducer;zustand;
	testing: testing-library;cypress;
	frameworks:next.js;remix.js
		
	back-end:
		postgrl
		rest
		jwt (autenticacao no geral)
		ORM (formas de acesso a db)(prisma)
		testes(como e porque existem)
		consumir api externas

	clean code (baixar livro clean code)
	git e github
	typescript (pode ser por ultimo)

ter bom networking:
	discord
	twiter
	meetups (online e presencial)

marca pessoal:
	github
		colocar seus objectivos, falar um pouco sobre mim
		colocar projectos alinhados com os meus objectivos
		ter consistencia e recorrencia

	linkedin
		criar boa rede de conexoes (pessoas que admira, outros devs)
		perfil simples (objectivos, tecnologias que estudo)
		publicar estudos, testes, projectos, etc.